#### Setup

1. Add .env file in the root;
2. Add MONGO_URI with correct value;
3. Add JWT_SECRET with correct value;
4. Add JWT_LIFETIME with correct value (optional, 1h by default).
5. Add PORT (optional, 3000 by default)

```bash
npm install && npm run build && npm start
```
